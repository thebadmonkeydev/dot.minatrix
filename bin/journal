#!/usr/bin/env bash
#
# journal
#
#
JOURNAL_ROOT='/home/michael/Desktop/journal'

if [[ -z "${@// }" ]];then
  CURRENT_FILE_PATH=$JOURNAL_ROOT/$(date +%F).md
else
  CURRENT_FILE_PATH=$JOURNAL_ROOT/$(date +%F)-$@.md
fi

eval $EDITOR $CURRENT_FILE_PATH
